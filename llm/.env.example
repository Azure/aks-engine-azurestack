# Azure LLM Configuration
# These are optional - the client will use defaults if not provided

# Azure LLM endpoint (optional, defaults to https://models.github.ai/inference)
# AZURE_LLM_ENDPOINT=https://models.github.ai/inference

# Azure LLM model (optional, defaults to openai/gpt-4.1)
# AZURE_LLM_MODEL=openai/gpt-4.1

# GitHub token (required)
GITHUB_TOKEN=your_github_token_here
